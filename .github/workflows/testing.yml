name: Testing

on:
  push:
    branches:
      - "1.20.1"
  pull_request:
  workflow_dispatch:

jobs:
  testing:
    uses: JensenJ-Modding/Mod-Resources/.github/workflows/testing-forge-fabric.yml@main
    with:
      gradle-version: "8.11"
      java-version: 17
      fabric-mods: ""
      forge-mods: ""